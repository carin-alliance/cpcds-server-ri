version: "3"
services:
  cpcds-server-ri:
    image: cpcds-write
    container_name: cpcds-server-ri
    restart: on-failure
    ports:
      - "8080:8080"
    volumes:
      - ./data:/var/lib/jetty/target
